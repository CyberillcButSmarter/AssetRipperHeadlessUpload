# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://help.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2
updates:
  - package-ecosystem: "nuget" # See documentation for possible values
    directory: "/" # Location of package manifests
    schedule:
      interval: "daily"
    open-pull-requests-limit: 20
    ignore:
      - dependency-name: "Microsoft.NET.ILLink.Tasks"
      - dependency-name: "Microsoft.DotNet.ILCompiler"
      - dependency-name: "Samboy063.Cpp2IL.Core"
      - dependency-name: "AssetRipper.Cpp2IL.Core"
      # Source generators must run on the host compiler. Bumping this above the
      # Roslyn shipped in our build SDK (10.0.x) makes the generators fail to load
      # (CS9057) and breaks the whole build. Pinned to 5.0.0 to match the SDK.
      - dependency-name: "Microsoft.CodeAnalysis.CSharp"
    groups:
      NUnit:
        patterns:
          - "NUnit*"
          - "Microsoft.NET.Test.Sdk"
          - "coverlet.collector"
      AsmResolver:
        patterns:
          - "AsmResolver.*"
          - "AssetRipper.CIL"

  - package-ecosystem: "github-actions"
    # Workflow files stored in the
    # default location of `.github/workflows`
    directory: "/"
    schedule:
      interval: "daily"
